rapidfuzz
free-proxy
PySocks
git+https://github.com/BartolomeoRusso9/SpotiFLAC-Module-Version.git@7ace1b123b59555543b4f5476e2dc07561df42c6#egg=SpotiFLAC
# SpotiFLAC 1.6.0 hard-imports pydoll (core/solver.py, core/signed_session_mono.py,
# reached from providers/amazon.py at import time) but only declares it in its own
# requirements.txt, not in pyproject dependencies — so pip does not pull it in.
pydoll-python>=2.9.0
nodriver>=0.36
pycryptodomex
httpx[http2]>=0.27.0
hishel
socksio
aiofiles>=23.2.1
tenacity>=8.3.0
tqdm>=4.66.0
pywebview>=6.2.1
mutagen>=1.47.0
pydantic>=2.7.0
cryptography>=42.0.0,<47
packaging>=23.0
pycryptodome>=3.20.0
yt-dlp>=2024.04.09
imageio-ffmpeg>=0.5.1
python-dotenv>=1.0.0
GitPython>=3.1.43
countrystatecity-countries>=1.0.2

pyobjc-framework-IOBluetooth>=10.0; sys_platform == "darwin"
pyobjc-framework-AVFoundation>=12.2; sys_platform == "darwin"
pyobjc-framework-CoreAudio>=12.2; sys_platform == "darwin"
bleak>=0.22.3; sys_platform == "win32"
pythonnet>=3.0.4; sys_platform == "win32"
sounddevice>=0.5.1; sys_platform == "win32"
soundfile>=0.12.1; sys_platform == "win32"
numpy>=1.26.0; sys_platform == "win32"
torrfetch
libtorrent; sys_platform != "win32"

# Duck.ai AI-reranker browser worker (ddg_browser.py). Requires a one-time
# browser download per venv:  python -m playwright install chromium
playwright>=1.49
playwright-stealth>=2.0.0
